#!/usr/bin/env python

from distutils.core import setup

setup(name='pathAdapt',
      version='0.0',
      description='sotfware to simlute allele trajectories under selection due to epidemics',
      author='Lawrence Uricchio',
      author_email='uricchil@gmail.com',
      url='TBD',
      packages=['pathAdapt'],
      package_dir={'pathAdapt' : 'src/pathAdapt'},
     )
